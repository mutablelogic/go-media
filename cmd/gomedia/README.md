# Tags

* `--tag cli` - build the self-contained command line interface (no server-side code)
* `--tag client` - build the client-side code (no server-side code)

Otherwise, the default build will include both client and server code, but no CLI.
